sweep_id: basic_gru${now:%Y-%m-%d}-${now:%H-%M-%S}

task/data: small, medium
task/model: gru5M, gru10M
task.data.context_length: [200,200]
seed: 15
model_checkpoint: base

max_steps: 10000000000000000 # won't reach the max, seen_tokens is our stopping condition
max_tokens: 1000000000 # 1B