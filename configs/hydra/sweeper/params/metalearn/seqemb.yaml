sweep_id: metalearn_seqemb_${now:%Y-%m-%d}-${now:%H-%M-%S}

task/metalearn: base

task/metalearn/data: small
task/metalearn/model: ctx/seqemb
task.metalearn.model.encoder.pretrained_id:
seed: 15

model_checkpoint: base
max_steps: 50000 # won't reach the max, max_tokens is our stopping condition