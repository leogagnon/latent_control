_target_: models.base.MetaLearner
tag: explicit

dec_cfg:
  _target_: models.decoder.TransformerDecoder
  tag: medium_gpt
  block_size: 300
  out_size : ${eval:'${...data.n_obs} + 1'}
  n_layer: 6
  n_head: 8
  n_embd: 512
  dropout: 0.0
  bias: True
  positional_encodings: True

enc_cfg:
  _target_: models.encoder.KnownEncoder
  n_embd: 512
  latents_shape: ???
