sweep_id: start_at_n_real${now:%Y-%m-%d}-${now:%H-%M-%S}

task/data: medium
task/model: gpt5M, mamba5M
task.data.context_length: [200,200]
task.data.start_at_n: null, 100
seed: 15,16,17
model_checkpoint: base

max_steps: 20000 # won't reach the max, seen_tokens is our stopping condition
max_tokens: 800000000 # 1B